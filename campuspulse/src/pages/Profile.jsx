import PageShell from'../components/PageShell';import{useAuth}from'../context/AuthContext';export default function Profile(){const{currentUser}=useAuth();return <PageShell><section className="container-page py-12"><div className="rounded-3xl border bg-white p-8"><div className="grid h-20 w-20 place-items-center rounded-full bg-violet-100 text-2xl font-black text-primary">{currentUser?.name?.slice(0,2).toUpperCase()}</div><h1 className="mt-5 text-3xl font-black">{currentUser?.name}</h1><p className="mt-1 text-gray-500">{currentUser?.role} · CampusPulse member</p><div className="mt-8 grid gap-4 sm:grid-cols-2"><div className="rounded-xl bg-gray-50 p-4"><span className="text-xs text-gray-500">Email</span><p className="font-semibold">{currentUser?.email}</p></div><div className="rounded-xl bg-gray-50 p-4"><span className="text-xs text-gray-500">Campus</span><p className="font-semibold">{currentUser?.college||'Not provided'}</p></div></div></div></section></PageShell>}
